CC = gcc
CFLAGS = -Wall -O2 -I .
LDLIBS = -lm

PROG = calc
PROG_OBJS = main.o calc.o stack.o echo_console.o

all:$(PROG)

calc: $(PROG_OBJS)
	$(CC) $(CFLAGS) -o $(PROG) $(PROG_OBJS) $(LDLIBS)

clean:
	rm -rf *.o $(PROG) *~ *.swp

